# v0.9.1 and beyond — open work tracker

**Purpose:** One place to see everything still open: Copilot audit deferrals,
in-flight branch work not yet on `develop`, voice Phase B (Discord), and
older product backlog items.

**Merge discipline:** Nothing below authorizes a merge to `develop`. Per
`.cursorrules`, a `v0.9.1-release-prep.md (or later) must name each
`feature/*` branch by exact name before merge.

**Last updated:** 2026-04-21

---

## 1. Branch in flight (not on `develop`)

| Item | Notes |
|------|--------|
| **`feature/v0.9.1-copilot-backlog`** | Partial implementation of `v0.9.1-copilot-backlog.md` (commit message on branch summarizes scope). **Not merged** until release prep names this branch. |

### Already implemented on that branch (merge when authorized)

These were **open** in the Copilot backlog doc; they are **done on the
branch** and will leave the “still open” list once the branch merges:

| ID | Summary |
|----|---------|
| **H-5** | Dummy `model_name` guard + `HESTIA_ALLOW_DUMMY_MODEL` + root `tests/conftest.py` |
| **H-6** | Module-level logger in `DefaultPolicyEngine` |
| **H-7** | `Turn.artifact_handles`, engine wiring, `delegate_task` propagation |
| **H-8** | `ConfirmCallback` typing + `CliAppContext.set_confirm_callback` |
| **H-10** | `egress_events` in `schema.py`; `TraceStore.create_table` no-op |
| **M-1** | FTS5 probe: narrow exceptions + log unexpected |
| **M-2** | Orchestrator: incremental `running_history` (fewer `get_messages` in tool loop) |
| **M-3** | Context builder: `asyncio.gather` for tool-pair token counts |
| **M-4** | Injection scanner: anchors + min length for role-prefix |
| **M-5** | Removed dead `IdentityCompiler._check_cache` |
| **M-6** / **M-12** | `src/hestia/policy/constants.py` (`CONTEXT_PRESSURE_THRESHOLD`, `PLATFORM_SUBAGENT`) |
| **M-8** | Public `set_confirm_callback` (replaces ad-hoc `app.confirm_callback = …`) |
| **M-9** | `SECURITY.md` + `from_file` docstring (documented, not sandboxed) |
| **M-10** | Doctor: `allowed_roots` + broad CWD (`$HOME` / `/`) note |
| **M-11** | Comment on per-delegation orchestrator / slot isolation |
| **A-5** | Scheduler passes `config.system_prompt` into `process_turn` |

---

## 2. Copilot backlog — still open after the branch above

Authoritative line items and file hints live in
[`../prompts/v0.9.1-copilot-backlog.md`](../prompts/v0.9.1-copilot-backlog.md).
This section is the **delta**: what remains **even if**
`feature/v0.9.1-copilot-backlog` merges as-is.

### Priority 2 — Medium (remaining)

| ID | Topic | Status |
|----|--------|--------|
| **M-7** | `email/adapter.py` — IMAP list order: sort by `INTERNALDATE` (or equivalent), not UID order | **Open** |
| **M-13** | `memory/store.py` — `list_memories` / dynamic `WHERE`: prefer SQLAlchemy Core bound style | **Open** |

### Priority 3 — Low (nitpick sweep)

| ID | Topic | Status |
|----|--------|--------|
| **L-1** | Duplicated `@tool` docstring boilerplate across `tools/builtin/*.py` | **Open** |
| **L-2** | `orchestrator/engine.py` — shorten long lines / drop unnecessary `# noqa: E501` | **Open** |
| **L-3** | Telegram `user_id` range check | **Open** — backlog marks *consider won’t-fix* for single-operator |
| **L-4** | `memory/store.py` — audit remaining `created_at` / `.isoformat()` for C-1-class timezone consistency | **Open** |
| **L-5** | `scheduler/engine.py` (or related) — `except …: pass` that should log | **Open** (confirm location) |
| **L-6** | `doctor.py` — Python version via `sys.version_info` | **Likely done** — verify and close when sweeping |
| **L-7** | `tools/registry.py` — document insertion-ordered `_tools` | **Done on branch** |
| **L-8** | `config.py` — small cache for `TrustConfig.for_trust` inverse lookup | **Open** |

### Priority 4 — Test gaps (T-1 shipped in v0.9.0)

| ID | Topic | Status |
|----|--------|--------|
| **T-2** | Orchestrator — `PolicyFailureError` classification through `process_turn` | **Open** |
| **T-3** | Orchestrator — `MaxIterationsError` carries iteration metadata | **Open** |
| **T-4** | Artifacts — concurrent / idempotent `ArtifactStore.open()` against same root | **Open** |
| **T-5** | Email — STARTTLS refusal mock → `EmailAdapterError` | **Open** |
| **T-6** | Email — `SMTP_SSL` port 465 path with mock | **Open** |
| **T-7** | Policy — `_trust_for` fallback + warning when identity missing | **Open** |
| **T-8** | Memory — FTS5 vs `LIKE` fallback parity for a trivial query | **Open** |
| **T-9** | Inference — empty `choices` guard (H-1) | **Open** |
| **T-10** | Inference — non-dict tool `arguments` guard (H-2) | **Open** |

### Priority 5 — Architecture

| ID | Topic | Status |
|----|--------|--------|
| **A-1** | `CliAppContext` → `ServiceLocator` + `SessionContext` split | **Open** — defer to **v0.10.x** per backlog |
| **A-2** | DDL consolidation: `trace_store` / `memory/store` / `schema` (+ migrations story) | **Partially addressed** by H-10 on branch for traces/egress; **memory** and full migration story **still open** |
| **A-4** | Split `commands.py` into `src/hestia/cli/` | **Open** — defer to **v0.10.x** per backlog |
| **A-5** | Scheduler uses configured `system_prompt` | **Done on branch** |

### Won’t-fix / doc-only (track as closed when merged)

| ID | Notes |
|----|--------|
| **M-9** | Arbitrary Python in config — **documented** (`SECURITY.md` + `from_file` docstring on branch). |

| **L-3** | Explicitly optional / won’t-fix unless multi-tenant. |

---

## 3. Voice Phase B — Discord (product + env)

**Spec:** `docs/development-process/prompts/v0.8.0-release-and-voice-launch.md`
Track 5 Phase B (Discord replaces old Telegram MTProto userbot plan).

**Blocked on Dylan-side inputs** (same list as `KIMI_CURRENT.md`):

1. Discord bot token in `~/.hestia/.env` as **`HESTIA_DISCORD_TOKEN`**.
2. **`guild_id`** and **`voice_channel_id`** for the private Hestia server.
3. **Allowed-speaker Discord user IDs** (you + husband).
4. **Piper voice** choice for TTS.

**Process:** When those exist, you green-light Cursor; then a **release-prep
doc** must name the Phase B `feature/*` branches before any merge to
`develop` (per `.cursorrules`).

**Next step:** Walk through Discord setup together (bot creation, intents,
server IDs, env file layout) using the companion prompt as the technical
source of truth.

---

## 4. Older / non-Copilot backlog (from `KIMI_CURRENT.md`)

| ID | Description |
|----|-------------|
| **L39** | `hestia upgrade` — auto-fix or guided repair from `hestia doctor` findings |
| **L44** | Dogfooding journal rollup after sustained real use of v0.9.x |
| **L43 (legacy)** | Original Telegram MTProto “voice calls” plan — **superseded** by Discord Phase B; keep docs for history only |

---

## 5. Process / meta

| Item | Notes |
|------|--------|
| **`v0.9.1-release-prep.md`** | Does **not** exist yet. Required before merging `feature/v0.9.1-copilot-backlog` and/or Discord Phase B branches. |
| **`KIMI_CURRENT.md`** | May still mention “first loop H-5” as a warm-up; after branch merge, refresh to match reality. |
| **Capability audit** | [`capability-audit-april-17.md`](capability-audit-april-17.md) is a separate review artifact; not the same as Copilot backlog unless you want items folded in here later. |

---

## 6. Suggested order after Discord env is stable

1. **Release prep** for whatever train merges next (name branches explicitly).
2. **Merge** `feature/v0.9.1-copilot-backlog` if scope includes it.
3. **M-7 + T-5 + T-6** (email batch).
4. **M-13 + T-8** (memory batch).
5. **T-2, T-3, T-9, T-10** (orchestrator + inference tests).
6. **L-sweep** (L-1, L-2, L-4, L-5, L-8) as one small loop.
7. **A-2** remainder, then **A-1 / A-4** on a **v0.10.x** train.

---

*When this tracker goes stale, either update this file in place or add a
new dated tracker and link it from `kimi-loop-log.md`.*
