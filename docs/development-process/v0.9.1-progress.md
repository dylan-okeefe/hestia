# v0.9.1 Progress Tracker

**Release branch:** `release/v0.9.1` (branched from `develop` at `v0.9.0`)

**Purpose:** Live dashboard of what's done, what's merged, and what's left for the v0.9.1 release. Updated after every merge to `release/v0.9.1`.

---

## Merge order to `release/v0.9.1`

| # | Branch | Status |
|---|--------|--------|
| 1 | `feature/v0.9.1-copilot-backlog` | ✅ Merged |
| 2 | `feature/v0.9.1-email-cleanup` | ✅ Merged |
| 3 | `feature/v0.9.1-memory-cleanup` | ✅ Merged |
| 4 | `feature/v0.9.1-policy-and-inference` | ✅ Merged |
| 5 | `feature/v0.9.1-remaining-highs` | ⏭️ Skipped (duplicate of #1) |

---

## Completed items (already on `release/v0.9.1`)

### Priority 1 — High severity
- [x] **H-5** — reject `model_name == "dummy"` at config-load
- [x] **H-6** — module-level logger in `DefaultPolicyEngine`
- [x] **H-7** — propagate subagent `artifact_handles` through `delegate_task`
- [x] **H-8** — tighten `confirm_callback` type to `ConfirmCallback | None`
- [x] **H-10** — consolidate duplicate `traces` DDL into `schema.py`

### Priority 2 — Medium severity
- [x] **M-1** — narrow FTS5 probe exceptions + log unexpected
- [x] **M-2** — keep history in memory across tool-call iterations
- [x] **M-3** — parallelize `_count_tokens` with `asyncio.gather`
- [x] **M-4** — tighten injection scanner regex (anchor + min length)
- [x] **M-5** — remove dead `_check_cache(source_hash)` code
- [x] **M-6** — extract `CONTEXT_PRESSURE_THRESHOLD` constant
- [x] **M-7** — sort IMAP `list_messages` by `INTERNALDATE`
- [x] **M-9** — `SECURITY.md` note on arbitrary Python in config
- [x] **M-10** — `hestia doctor` warns on `allowed_roots=["."]`
- [x] **M-11** — document fresh orchestrator per delegation
- [x] **M-12** — promote "subagent" to `PlatformName` constant
- [x] **M-13** — memory store param-binding consistency

### Priority 4 — Test gaps
- [x] **T-2** — `PolicyFailureError` propagation test
- [x] **T-3** — `MaxIterationsError` carries iteration count
- [x] **T-7** — `_trust_for(session)` fallback test
- [x] **T-8** — FTS5 vs LIKE fallback parity
- [x] **T-9** — empty-`choices` guard test
- [x] **T-10** — non-dict-`arguments` guard test

### Architecture
- [x] **A-5** — scheduler respects `config.system_prompt`

---

## Remaining work

### Priority 2 — Medium severity (1 item)
- [x] **M-8** — `ReflectionScheduler.wire_failure_handler()` public method (replaces direct access to `sched._record_failure`)

### Priority 3 — Low severity / nitpick sweep (batch into one loop)
- [x] **L-1** — deduplicate docstring boilerplate in `tools/builtin/*.py`
- [x] **L-2** — shorten long lines with `# noqa: E501` (none found)
- [x] **L-3** — Telegram `user_id` range check (won't-fix — no runtime validation needed)
- [x] **L-4** — audit remaining `.isoformat()` sites for naive-datetime bugs (none found — all use `utcnow()`)
- [x] **L-5** — scheduler `except TimeoutError: pass` → `contextlib.suppress(TimeoutError)`
- [x] **L-6** — doctor Python-version detection use `sys.version_info` (already correct)
- [x] **L-7** — document insertion-ordered dict iteration in `tools/registry.py` (already present)
- [x] **L-8** — cache `TrustConfig` presets and `HestiaConfig.for_trust` lookup

### Priority 5 — Architecture (defer to v0.10.x)
- [ ] **A-1** — split `CliAppContext` into `ServiceLocator` + `SessionContext`
- [ ] **A-2** — consolidate all inline DDL into `schema.py` + migrations
- [ ] **A-4** — split `commands.py` into `cli/` sub-modules

---

## Feature branches status

| Branch | Status | Action |
|--------|--------|--------|
| `feature/v0.9.1-copilot-backlog` | Merged to release | Can delete after release |
| `feature/v0.9.1-email-cleanup` | Merged to release | Can delete after release |
| `feature/v0.9.1-memory-cleanup` | Merged to release | Can delete after release |
| `feature/v0.9.1-policy-and-inference` | Merged to release | Can delete after release |
| `feature/v0.9.1-remaining-highs` | Duplicate — skipped | Can delete now |
| `feature/v0.9.1-app-and-config` | Empty (at develop tip) | Can delete now |

---

## Release checklist

- [x] All remaining items above completed or explicitly deferred
- [x] `release/v0.9.1` merged to `develop`
- [x] Version bumped to `0.9.1`
- [x] CHANGELOG updated
- [x] Annotated tag `v0.9.1` placed
- [x] `main` merged to `develop` (merge commit; fast-forward not possible due to prior release merges)
- [ ] Dylan pushes to origin
