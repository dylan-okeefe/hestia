# v0.9.1 release prep

**Target version:** v0.9.1
**Tag at:** `develop` tip after `release/v0.9.1` merges.
**Written:** 2026-04-20

## In-scope branches

- `release/v0.9.1` — integration branch containing:
  - `feature/v0.9.1-copilot-backlog` — 39-item audit backlog from v0.9.0
    (H-5, H-6, H-7, H-8, H-10, M-1 through M-7, M-9 through M-13,
    T-2, T-3, T-7 through T-10, A-5).
  - `feature/v0.9.1-email-cleanup` — email adapter hardening and
    config-load validation.
  - `feature/v0.9.1-memory-cleanup` — memory store consistency fixes
    and param-binding cleanup.
  - `feature/v0.9.1-policy-and-inference` — policy engine and inference
    client fixes from the audit bundle.

## Out of scope (deferred to v0.10.x)

- **A-1** — split `CliAppContext` into `ServiceLocator` + `SessionContext`
- **A-2** — consolidate all inline DDL into `schema.py` + migrations
- **A-4** — split `commands.py` into `cli/` sub-modules
- **L-3** — Telegram `user_id` range check (won't-fix)

## Out of scope (remains on feature branch)

- `feature/voice-phase-b-discord` — Discord voice adapter. Sub-scope A
  (voice join + STT logging) is complete; sub-scopes B/C (orchestrator
  wiring, TTS outbound, semantic turn detection, barge-in) are deferred
  pending Dylan prioritization.

## Release sequence

1. `release/v0.9.1` merged to `develop`.
2. `pytest` / `mypy` / `ruff` green on `develop`.
3. Version bumped to `0.9.1` in `pyproject.toml` and `src/hestia/__init__.py`.
4. CHANGELOG updated with `[0.9.1]` entry.
5. Annotated tag `v0.9.1` placed on `develop` tip.
6. `main` fast-forwarded to `develop`.
7. Push commands printed for Dylan (Cursor does not push).
