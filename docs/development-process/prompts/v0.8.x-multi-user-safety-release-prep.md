# v0.8.x Multi-user safety release prep

**Scope:** L45a → L45b → L45c multi-user safety train.

**Merge discipline:** Per `.cursorrules` post-release merge discipline, these
feature branches do NOT merge to `develop` until this release-prep doc names
them in scope and a release tag is placed.

## Contents

- L45a: trust + identity plumbing (runtime ContextVars, per-user trust overrides)
- L45b: FTS5 memory migration + user-scoped memory queries/tools/epochs
- L45c: multi-user docs + allow-list role-shape hardening

## Target version

v0.8.1 or v0.8.2 depending on dogfooding feedback.
