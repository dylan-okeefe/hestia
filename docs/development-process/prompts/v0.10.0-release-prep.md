# v0.10.0 release prep

**Target version:** v0.10.0
**Tag at:** `develop` tip after merge sequence.
**Written:** 2026-04-21

## In-scope branches

- `chore/l47-adr-normalization` — consolidate hybrid ADR system into `docs/adr/` with 4-digit zero-padded numbering; move design doc to `docs/design/`.
- `feature/l48-config-consistency` — add `_ConfigFromEnv` mixin across all config classes; normalize tool factory signatures; harden config validation (negative max_tokens, unparseable cron, malformed JSON).
- `feature/l49-orchestrator-extract-methods` — decompose `process_turn` from ~390 lines to 98 lines; extract `_run_inference_loop`, `_finalize_turn`, `_record_failure_if_enabled`, `_safe_transition`; add `IllegalTransitionError` guards.
- `feature/l50-commands-split` — split `src/hestia/commands.py` (~1112 lines) into `src/hestia/commands/` package by domain.
- `feature/l51-missing-test-coverage` — add tests for `platforms/runners.py`, `memory/epochs.py`, `voice/vad.py`, `reflection/prompts.py`; add `assert_called_once()` assertions to orchestrator tests; fix e2e conftest response accumulation.
- `feature/l52-context-builder-decomposition` — extract `HistoryWindowSelector` and `CompressedSummaryStrategy` from `ContextBuilder.build`; thin `build` from ~215 lines to 78 lines.

## Merge sequence

1. `chore/l47-adr-normalization` → `develop` (docs-only, safe to land first).
2. `feature/l51-missing-test-coverage` → `develop` (tests-only, establishes harness).
3. `feature/l48-config-consistency` → `develop`.
4. `feature/l50-commands-split` → `develop`.
5. `feature/l49-orchestrator-extract-methods` → `develop`.
6. `feature/l52-context-builder-decomposition` → `develop`.
7. `pytest` / `mypy` / `ruff` green on `develop`.
8. Version bumped to `0.10.0` in `pyproject.toml` and `src/hestia/__init__.py`.
9. CHANGELOG updated with `[0.10.0]` entry summarizing the above.
10. Annotated tag `v0.10.0` placed on `develop` tip.
11. `main` fast-forwarded to `develop`.
12. Push commands printed for Dylan (do not push from agent).

## Pre-merge checklist

- [ ] All 6 branches rebased onto current `develop` tip.
- [ ] `pytest tests/unit/ tests/integration/ tests/cli/ tests/docs/ -q` green.
- [ ] `mypy src/hestia` at baseline (7 pre-existing errors in `config.py` DiscordVoiceConfig env parsers).
- [ ] `ruff check src/` at baseline (25 errors).
- [ ] CHANGELOG entry written.
- [ ] No leaked secrets in any branch.

## Out of scope (remains on feature branch)

- `feature/discord-voice-iteration` — Discord voice adapter iteration (L46). Deferred pending Dylan prioritization and runtime testing.
- `feature/v0.9.1-remaining-highs` — v0.9.1 release-prep branch with unmerged H-5 through H-10 fixes. Either merge into v0.9.1 first, or rebase onto v0.10.0 if v0.9.1 ships before this prep doc executes.
