# L80 — v0.11.0 Release Prep & Engine Paydown

**Status:** Spec only  
**Branch:** `feature/l80-v0.11.0-release-prep-and-engine-paydown` (from `develop`)

## Goal

Ship v0.11.0: apply the remaining must-fix tag blockers from the post-cleanup re-evaluation and pay down the last of the orchestrator engine's one-line delegate debt.

## Review carry-forward

- *(none — this is a clean-up loop)*

## Scope

### §1 — Replace CliAppContext annotations with AppContext

The `CliAppContext = AppContext` alias exists for backward compatibility with external consumers. First-party code should use the canonical name.

**Files:**
- `src/hestia/cli.py` — replace import and type annotations
- `src/hestia/commands/history.py` — replace import and type annotations

**Commit:** `refactor(cli,history): use AppContext instead of CliAppContext alias in first-party code`

### §2 — Update CHANGELOG and version bump

**CHANGELOG.md:**
Add `[0.11.0]` section at the top covering the L60–L78 arc. Key items:
- Security hardening: deny-all `allowed_roots` default, credential redaction in `__repr__`, developer preset guardrails, terminal sandboxing, egress URL redaction
- Structural: InferenceClient consolidation (`_request()` helper, context manager), app context collapse to single `AppContext` class, SlotManager correctness (batch LRU, lock release), meta-tool dispatch table
- Reliability: bounded caches (`_tokenize_cache`, `_last_edit_times`), tool-call-per-turn cap, concurrent tool exception shielding, startup config validation
- UX: `hestia history` command, improved user-facing error messages
- Cleanup: skills subsystem removed (~1,500 lines deleted)

**pyproject.toml:**
Bump version from `0.10.0` to `0.11.0`.

**Commit:** `docs(release): CHANGELOG v0.11.0 and version bump`

### §3 — docs/development-process/ discoverability

Add a note to `docs/README.md` explaining that `docs/development-process/` contains internal loop specs, handoffs, and reviews — development archaeology, not user-facing documentation. Point readers to `docs/guides/` and `docs/adr/` for operational docs.

**Commit:** `docs: clarify docs/development-process/ as internal development record`

### §4 — Inline engine.py one-line delegates

`engine.py` has 5 methods that are single-line wrappers around phase classes:
- `_prepare_turn_context` → `self._assembly.prepare(...)`
- `_handle_context_too_large` → `self._finalization.handle_context_too_large(...)`
- `_handle_unexpected_error` → `self._finalization.handle_unexpected_error(...)`
- `_run_inference_loop` → `self._execution.run(...)`
- `_finalize_turn` → `self._finalization.finalize_turn(...)`

Inline these calls directly into `process_turn`. Remove the private methods and their docstrings. Target: `engine.py` from ~305 lines to ~260 lines.

**Commit:** `refactor(orchestrator): inline one-line engine delegates into process_turn`

## Tests

- No new tests needed — pure refactoring and docs.
- Keep existing tests green.

## Acceptance

- `pytest tests/unit/ tests/integration/ -q` green
- `mypy src/hestia` reports 0 errors
- `ruff check src/` remains at baseline or better
- `.kimi-done` includes `LOOP=L80`

## Handoff

- Write `docs/handoffs/L80-v0.11.0-release-prep-and-engine-paydown-handoff.md`
- Update `docs/development-process/kimi-loop-log.md`
- Advance `KIMI_CURRENT.md` to next queued item (or idle)
